# Claude Code ルール（okite-ai より）

## Less Is More

- 現在必要ない機能は実装しない（YAGNI）
- 複雑さより読みやすさを優先（KISS）
- 同じコードが3回出現するまで抽象化しない
- 「将来のため」という理由でコードを追加しない

## コーディング前に学ぶ

- 新しいコードを書く前に、同じレイヤー・種類の既存実装を2〜3個確認する
- 構造・命名規約・DI方法・エラー処理・インポートスタイルを既存コードに合わせる
- プロジェクト固有のパターンを尊重し、標準的なベストプラクティスより優先する

## イミュータビリティを優先する

- オブジェクトを変更するのではなく、新しいインスタンスを返す
- 関数の引数を変更しない
- グローバルな状態の変更を避ける
- 例外：大量データのバッチ処理、ローカル一時変数、明示的に文書化されたパフォーマンス最適化

## 曖昧なサフィックスを避ける

以下のサフィックスは使用しない：
- `Manager`、`Util`、`Facade`、`Service`、`Runtime`、`Engine`

代わりに責務が明確な名前を使う：
- データ管理 → `*Registry`、`*Store`
- 選択・分岐 → `*Policy`、`*Selector`
- 生成 → `*Factory`、`*Builder`

## 1ファイル1公開型

- 1つの公開クラス・型につき1つのファイルを作成する
- ファイル名は型名を反映する（スネークケース）
- 既存ファイルへの公開型の追加は禁止
- 例外：プライベート実装型やネスト型の同居は許可

## スキル選択の説明

- スキルを呼び出す前に、選択したスキル名と理由をユーザーに説明する
- 複数候補がある場合は比較して説明する
